mutation MintToken(
  $collectionId: BigInt!
  $params: MintTokenParams!
  $recipient: String!
) {
  MintToken(
    collectionId: $collectionId
    params: $params
    recipient: $recipient
  ) {
    id
  }
}
