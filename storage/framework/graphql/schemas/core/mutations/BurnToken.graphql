mutation BurnToken(
  $collectionId: BigInt!
  $params: BurnParamsInput!
  $signingAccount: String
) {
  Burn(
    collectionId: $collectionId
    params: $params
    signingAccount: $signingAccount
  ) {
    id
  }
}
